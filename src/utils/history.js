const KEY='yyxvt:history';const LIMIT=40;function read(){if(typeof window==='undefined')return[];try{const v=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(v)?v:[]}catch{return[]}}function write(list){try{localStorage.setItem(KEY,JSON.stringify(list.slice(0,LIMIT)));return true}catch{return false}}export function getHistory(){return read()}export function saveHistory(item,episode=null){if(!item?.id||typeof window==='undefined')return;const list=read().filter(e=>String(e.id)!==String(item.id));list.unshift({id:item.id,title:String(item.title||'').slice(0,300),poster:String(item.poster||'').slice(0,2048),typeName:String(item.typeName||'').slice(0,100),episode:String(episode?.label||'').slice(0,100),url:String(episode?.url||'').slice(0,4096),updatedAt:Date.now()});write(list)}export function removeHistory(id){write(read().filter(e=>String(e.id)!==String(id)))}export function clearHistory(){try{localStorage.removeItem(KEY)}catch{}}